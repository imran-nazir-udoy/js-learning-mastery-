let str="imrannazirudoy";
console.log(str);//
let k=str.toUpperCase();//as it provides new value so we have to store it na new variable
//same goes to toLpeerCase
//in javascript javascript is immutable it s not changed we have to put in other variable
console.log(k)


//trim method remove spaces from front and back
let t="     hey hey ho ho ha ha     "
console.log(t.trim())

//str,slice(start, end+1)//for making substring end is exclusive

//str.concat(str2) //for joining

//str.replace(oldvalue , newvalue)

//str.charAt(idx)

